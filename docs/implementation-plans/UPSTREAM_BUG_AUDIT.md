# Upstream ScanCode Bug Audit

Status: 🟡 Active cross-cutting audit reference

This document keeps a Provenant-side watchlist for upstream ScanCode bugs and open PRs that still matter to us. It is intentionally action-first: the main table below is ordered from highest to lowest priority, folds related upstream issues and PRs into the same row, and only keeps work that still looks unresolved or still merits targeted investigation on the Rust side.

The trailing table is the review residue: upstream bugs and PRs that were checked, but either do not apply to Provenant, are already covered locally, or are being intentionally held for compatibility reasons.

## How to Work an Item

For both humans and agents, the expected workflow is:

1. Read the upstream issue and any relevant upstream PRs before changing code.
2. Study the Provenant side deeply first: related local issues, current code paths, existing tests, and any nearby plan/improvement docs.
3. Reproduce the problem locally where practical, then turn that reproducer into the smallest durable regression coverage that fits the change: unit, parser/integration, scanner-level, or golden tests.
4. Implement the fix or feature end-to-end rather than stopping at partial plumbing.
5. Run the narrowest relevant verification for the changed surface, including golden tests when output parity or fixture-backed behavior is the point of the change.
6. If Rust intentionally diverges from current upstream behavior, document that decision clearly instead of leaving it implicit.

## Recommended Provenant Follow-up

Rows are ordered from highest to lowest priority.

| Work item                                                                                  | Upstream signals               | Provenant issue                                         | Current Provenant read                                                                                                                                                                                                                        | Recommended next step                                                                                                                                                   |
| ------------------------------------------------------------------------------------------ | ------------------------------ | ------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Verify declared-license handling for short composite expressions such as `MIT-0 OR ...`    | Issue `#4574`; open PR `#4691` | [#540](https://github.com/mstykow/provenant/issues/540) | Provenant has generic expression precedence coverage, but not the exact Cargo-style reproducer from upstream. The open PR is the strongest concrete lead for how upstream is addressing this class of false normalization.                    | Import the upstream reproducer into Cargo parser tests and compare current Rust output before deciding whether rule or normalization changes are needed.                |
| Verify that `See LICENSE`-style references do not degrade into `unknown-license-reference` | Issue `#4481`; open PR `#4671` | [#543](https://github.com/mstykow/provenant/issues/543) | Provenant already has local-file reference handling and `unknown-license-reference` plumbing, but no issue-specific reproducer proving that obvious MIT text plus `See LICENSE` is handled correctly with the current embedded rule snapshot. | Port the upstream fixture or a minimized equivalent into license golden tests. If reproduced, use the PR's `referenced_filenames` direction as the likely fix strategy. |
| Check for BSD false positives that over-detect `BSD-3-Clause AND BSD-2-Clause`             | Issue `#4718`                  | [#541](https://github.com/mstykow/provenant/issues/541) | This still looks plausible because Provenant uses embedded upstream-derived rule data and no issue-specific regression fixture was found locally.                                                                                             | Add the upstream sample as a license golden test and verify whether current embedded rule data still triggers the false positive.                                       |
| Reproduce BusyBox copyright corruption and adjacent noisy-text false positives             | Issue `#4867`; open PR `#4855` | [#544](https://github.com/mstykow/provenant/issues/544) | The BusyBox-specific bug is not yet covered locally, and the adjacent open PR about unicode / non-meaningful text suggests a broader copyright false-positive cluster worth checking while reproducing BusyBox.                               | Add the BusyBox reproducer first, then add one or two garbage-text / unicode-noise cases if the same failure mode seems plausible in Rust.                              |
| Add CLI-level regressions for ignore-pattern edge cases                                    | Issues `#4212`, `#4459`        | [#547](https://github.com/mstykow/provenant/issues/547) | Provenant's unit-level shaping and path-selection logic looks reasonable, but there is still no issue-specific end-to-end CLI coverage for ignored directories and root-level glob interactions.                                              | Add narrow CLI integration tests for ignored directories and root-level file globs such as `--ignore "build/*"` and `--ignore "*.csv"`.                                 |
| Decide whether parser dispatch should isolate panics instead of aborting the chain         | Issue `#3971`                  | [#548](https://github.com/mstykow/provenant/issues/548) | `try_parse_file()` still relies on individual parsers not panicking; the dispatcher does not currently isolate parser panics with `catch_unwind`. This is a robustness question more than a confirmed parity bug.                             | Audit realistic parser panic risk first, then decide whether dispatcher-level isolation is worth the trade-off.                                                         |
| Consider a dedicated Yarn Plug-and-Play `.pnp.cjs` parser                                  | Open PR `#4875`                | [#539](https://github.com/mstykow/provenant/issues/539) | Provenant already assigns root-level `.pnp.cjs` files to the correct npm/Yarn package during assembly, but it does not currently treat `.pnp.cjs` as its own metadata or dependency source.                                                   | Keep this as a lower-priority npm/Yarn feature opportunity unless package-manager metadata coverage becomes a near-term focus.                                          |
| Keep `CITATION.cff` and `publiccode.yml` in the planned structured-metadata bucket         | Open PRs `#4728`, `#4865`      | [#545](https://github.com/mstykow/provenant/issues/545) | These are real metadata surfaces we do not yet parse, but they are still lower-value than dependency and correctness work and already fit the planned structured-metadata scope in `package-detection/PARSER_PLAN.md`.                        | Revisit only after the higher-signal correctness and dependency items above.                                                                                            |

## Completed Work Items

| Work item                                                                                                | Upstream signals                                                                     | Provenant issue                                         | Resolution                                                                                                                                                                                                                                                                                                         |
| -------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ | ------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Make parser diagnostics reliably reach scan output                                                       | Issue `#2882`; closed PR `#2889`                                                     | [#546](https://github.com/mstykow/provenant/issues/546) | Audit confirmed Provenant already routes parser diagnostics into `FileInfo.scan_errors` via `parser_warn!` and `try_parse_file()`. Existing malformed-manifest scanner coverage plus output-layer regression tests now prove those diagnostics survive both file serialization and `headers[*].errors` projection. |
| Correct typoed `datasource_id` output spellings before first release, while keeping legacy input aliases | Issue `#4869`; open PR `#4870`; local typo audit of `nuget_nupsec` and `rpm_spefile` | [#542](https://github.com/mstykow/provenant/issues/542) | Fixed canonical serialization to `nuget_nuspec` / `rpm_specfile`, added legacy deserialization aliases for backward compatibility with `--from-json`, updated all fixtures, tests, and documentation. See PR for details.                                                                                          |

## Reviewed but Not Current Work Items

| Upstream signal                                                                        | Why it is not a current Provenant work item                                                                                                                                                                                                      |
| -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| Issue `#4753`; open PR `#4766` authenticated npm Git dependencies misparsed as aliases | Already covered locally. Provenant preserves the declared dependency key and treats Git, tarball, and file sources as unpinned requirements; see [`docs/improvements/npm-git-url-dependencies.md`](../improvements/npm-git-url-dependencies.md). |
| Issue `#3962` `pkg:jar` returned instead of `pkg:maven`                                | Already covered locally. Provenant's Maven-family parsers already emit Maven PURLs; see [`docs/improvements/maven-parser.md`](../improvements/maven-parser.md).                                                                                  |
| Issue `#3712`; open PR `#4711` strip-root path shaping                                 | Already covered locally. Provenant already has explicit strip-root shaping coverage, so this does not need a separate backlog item.                                                                                                              |
| Issue `#3720` multiple explicit paths scan too much                                    | Already covered locally. Provenant already has dedicated multiple-input path-selection logic and tests.                                                                                                                                          |
| Issues `#2929`, `#2934`, `#2986` package/resource association bugs                     | Already covered locally. Provenant has broad `for_packages` assembly and scan coverage across ecosystems.                                                                                                                                        |
| Issues `#4039`, `#4722`, `#4724`, `#4760` recent copyright/author regressions          | Already mirrored by closed Provenant issues, so they do not belong in the active watchlist.                                                                                                                                                      |
| Issue `#4623`; open PR `#4762` SPDX exceptions should use `WITH`                       | Already covered locally. Provenant already has explicit `WITH` parsing, normalization, and SPDX mapping coverage.                                                                                                                                |
| Open PR `#4701` NuGet file-type license references                                     | Already covered locally. Provenant already captures `license_file` metadata and can read license files back from `.nupkg` archives; see [`docs/improvements/nuget-parser.md`](../improvements/nuget-parser.md).                                  |
| Open PR `#4700` include RPM release field in version                                   | Already covered locally. Provenant already builds full EVR versions, including `release`; see [`docs/improvements/rpm-parser.md`](../improvements/rpm-parser.md).                                                                                |
| Open PR `#4673` support `replace` in `go.mod`                                          | Already covered locally. Provenant already parses `replace` directives; see [`docs/improvements/go-parser.md`](../improvements/go-parser.md).                                                                                                    |
| Open PR `#4810` gemspec version constants stored literally                             | Already covered locally. Provenant already resolves narrow constant-based version references; see [`docs/improvements/ruby-parser.md`](../improvements/ruby-parser.md).                                                                          |
| Open PR `#4749` latest `package-lock.json` dependency object support                   | Already covered locally. Provenant already preserves modern non-version package-lock dependency specs instead of flattening them into invalid pinned versions; see [`docs/improvements/npm-yarn-parser.md`](../improvements/npm-yarn-parser.md). |
| Open PR `#4871` author extraction for `Author:Name` without a space                    | Already covered locally. Provenant already accepts optional whitespace after `Author:`.                                                                                                                                                          |
| Open PR `#4857` derive manifest discovery from registered handlers                     | Interesting architecture confirmation, but not an obvious local bug. Provenant already centralizes parser metadata generation from parser registration.                                                                                          |
| Issue `#4785`; open PR `#4786` CSV `end_line` fix                                      | Not currently applicable. Provenant does not currently ship CSV output.                                                                                                                                                                          |
